import React from 'react';
import cssClasses from './Loader.css';

const Loader = (props) => {
    return (
        <div className={cssClasses.Loader}>Loading...</div>
    );
}

export default Loader;
